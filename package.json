{
  "name": "@dctjs web components mono repo",
  "version": "1.0.2",
  "scripts": {
    "build": "npx nx build-prod @dctjs/web-components && npx nx build angular-workspace && npx nx build @dctjs/react",
    "build-storybook": "npx nx build-storybook @dctjs/web-components",
    "lint": "npx nx lint @dctjs/web-components",
    "storybook": "npx nx storybook @dctjs/web-components"
  },
  "private": true,
  "devDependencies": {
    "@nx/eslint-plugin": "^17.2.8",
    "@stylistic/eslint-plugin": "^1.5.0",
    "nx": "15.9.7",
    "typescript": "^5.3.3"
  },
  "workspaces": [
    "packages/*"
  ]
}
